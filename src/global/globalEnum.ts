export enum HTTPStatus {
  ERROR = 403,
  SUCCESS = 200,
  NOT_FOUND = 404,
}
export enum HTTPSMessage {
  ERROR = 'Server Internal Error',
  SUCCESS = 'Server Response Success',
  NOT_FOUND = 'Data Not Found',
}
